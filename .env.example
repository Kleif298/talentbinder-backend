# 🌍 BASE CONFIGURATION (.env)
# Copy this file to .env and fill in your values

# Server Configuration
PORT=4000
NODE_ENV=development

# JWT Configuration
JWT_SECRET=your_secret_key_here_minimum_32_characters
JWT_EXPIRE=60m

# ──────────────────────────────────────────────────────────

# 🔐 LDAP CONFIGURATION (.env.ldap)
# Create .env.ldap with these settings

LDAP_URL=ldap://your-ldap-server.local
LDAP_BASE_DN=dc=example,dc=local
LDAP_USERS_DN=cn=users,cn=compat,dc=example,dc=local
LDAP_ADMIN_DN=uid=admin,cn=users,cn=compat,dc=example,dc=local
LDAP_ADMIN_PASSWORD=your_ldap_admin_password

# Group names for authorization
LDAP_ADMIN_GROUP=admins
LDAP_USER_GROUP=users

# ──────────────────────────────────────────────────────────

# 🟢 DEVELOPMENT CONFIGURATION (.env.development)
# Create .env.development with these settings

DB_HOST=localhost
DB_PORT=5432
DB_USER=user
DB_NAME=talentBinder_db
DB_PASS=your_local_db_password

FRONTEND_URL=http://localhost:5173,http://localhost:4173

# ──────────────────────────────────────────────────────────

# ☁️ RENDER CLOUD CONFIGURATION (.env.render)
# Create .env.render with these settings

# Database URL (single connection string format)
DB_URL=postgresql://user:password@host.render.com:5432/database_name

# Frontend URL (your deployed frontend)
FRONTEND_URL=https://your-frontend.onrender.com

# Optional: Override JWT for cloud
# JWT_SECRET=different_secret_for_render

# ──────────────────────────────────────────────────────────

# 🚀 DAL PRODUCTION CONFIGURATION (.env.dal)
# Create .env.dal with these settings

# Production Database URL
DB_URL=postgresql://user:password@production-host.dal.com:5432/database_name

# Production Frontend URL
FRONTEND_URL=https://your-production-domain.com

# Production JWT Secret (MUST be strong and unique!)
JWT_SECRET=STRONG_PRODUCTION_SECRET_MINIMUM_32_CHARS

# Optional: Production LDAP if different
# LDAP_URL=ldap://production-ldap.dal.com
# LDAP_ADMIN_PASSWORD=production_ldap_password

