# 🚀 DAL PRODUCTION CONFIGURATION
# This is the final production environment when the app is released

# Production Database URL (single connection string)
DB_URL=postgresql://username:password@production-host.dal.com:5432/talentbinder_production

# Alternative: Individual database settings (if not using DB_URL)
# DB_HOST=production-host.dal.com
# DB_PORT=5432
# DB_USER=production_user
# DB_NAME=talentbinder_production
# DB_PASS=strong_production_password

# Frontend URL (production domain)
FRONTEND_URL=https://talentbinder.dal.com

# Production JWT Secret (MUST be changed to a strong secret!)
JWT_SECRET=CHANGE_THIS_TO_STRONG_PRODUCTION_SECRET_KEY_MINIMUM_32_CHARS

# Optional: Production-specific LDAP (if different from .env.ldap)
# LDAP_URL=ldap://production-ldap.dal.com
# LDAP_ADMIN_PASSWORD=production_ldap_password
