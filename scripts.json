{
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "description": "Diese Datei enthält alle npm Scripts für das Backend. Um diese zu nutzen, kopiere die Scripts in die package.json unter 'scripts' oder führe die Befehle direkt aus."
}
