# ☁️ RENDER CLOUD CONFIGURATION (Your own cloud server)
# Render provides database URL in single string format

# Database URL (contains host, port, user, password, database in one string)
DB_URL=postgresql://user:39aAi7lKM8nbByahiFJsNDMJVxBgFizG@dpg-d41kg2jipnbc73ffe7ng-a/talentbinder_db

# Frontend URL (your Render frontend deployment)
FRONTEND_URL=https://talentbinder-frontend.onrender.com

# Optional: Override JWT secret for cloud (recommended for production)
# JWT_SECRET=different_secret_for_render_environment